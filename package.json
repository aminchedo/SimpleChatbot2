{
  "name": "persian-ai-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run start",
    "install-deps": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}